一、httptask 过滤字段功能（按条件返回json对象） 功能说明
在 httptask 描述 栏中，输入想要返回的字段内容，例如 name,age,sex 多个字段之间用半角逗号分隔
且返回内容必须满足如下条件，否则不返回
	1、接口返回必须 json 格式
	2、字段唯一，即目标字段不能出现在 JSONArray 对象中
如 描述栏中 未输入 任何内容，默认不需要过滤，即返回全部
为保证数据安全性，如配置了过滤字段，httptask 所调用接口的返回内容  在bpm流程中处理完成，不落地

